export enum Accuracy {
  Minimum = 1,
  Low = 2,
  Balanced = 3,
  High = 4,
  Maximum = 5,
}

export enum ClientTypes {
  Subscriber = 'subscriber',
  Publisher = 'publisher',
}

export enum LocationUpdateType {
  Predicted = 'PREDICTED',
  Actual = 'ACTUAL',
}
